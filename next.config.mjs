/** @type {import('next').NextConfig} */
const nextConfig = {
    env: {
        BASE_URL: "https://bgsw-as-heybosch-p-eus-001.azurewebsites.net/api",    
        STT_LOCALES: "en-IN"
    }
};

export default nextConfig;
